package: "models"

meta:
  table_prefix: wz_

models:
  - name: organization
    relations:
      - model: user
        rel: belongsToMany
    definition:
      fields:
        - name: id
          type: int64
        - name: name
          type: string